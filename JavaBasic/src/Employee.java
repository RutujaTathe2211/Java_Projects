public interface Employee {
    //no method body only prototype is their //we can not create of object
    //static method not allowed but variables are static by default
    int minbalance=100;//static
   void employeeDetails();
    void companyDetails();
}
